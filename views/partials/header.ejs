<header class="site-header">
    <h2 class="logo">Ella Rises</h2>

    <nav class="navbar">
        <!-- Home always on the left -->
        <a href="/">Home</a>

        <!-- Events always visible -->
        <a href="/events">Events</a>

        <% if (!user) { %>
            <!-- NOT LOGGED IN -->
            <a href="/enroll">Enroll</a>
            <a href="/login">Login</a>

        <% } else { %>
            <!-- LOGGED IN USER -->

            <% if (user.ParticipantRole === "participant") { %>
                <!-- PARTICIPANT ROLE -->
                <a href="/donations">Donations</a>
                <a href="/profile/<%= user.Participant_ID %>">Profile</a>

            <% } else if (user.ParticipantRole === "admin") { %>
                <!-- ADMIN ROLE -->
                <a href="/donations">Donations</a>
                <a href="/participants">Participants</a>
                <a href="/dashboard">Dashboard</a>
                <a href="/create-user">Create User</a>
            <% } %>

            <!-- Logout always for logged-in users -->
            <a href="/logout">Logout</a>
        <% } %>
    </nav>
</header>
